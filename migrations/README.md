# Migration log

Future migrations use versioned filenames and must state their purpose, impact, compatibility approach, dry-run checks, rollback steps, and post-migration verification.

From 2.0.0 on, any new content involving the owner's personal understanding must first enter the immutable verbatim capture layer before derived views are generated.
